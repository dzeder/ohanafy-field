import { RoleTabPlaceholder } from '@/components/shared/RoleTabPlaceholder';

export default function Team(): React.ReactNode {
  return (
    <RoleTabPlaceholder
      screenName="Team"
      title="Team"
      description="Sales Manager view: rep performance, coverage gaps, recent visits across the territory."
    />
  );
}
