import { RoleTabPlaceholder } from '@/components/shared/RoleTabPlaceholder';

export default function Dispatch(): React.ReactNode {
  return (
    <RoleTabPlaceholder
      screenName="Dispatch"
      title="Dispatch"
      description="Driver Manager view: live truck positions, route assignments, exception triage."
    />
  );
}
