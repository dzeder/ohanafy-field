import { RoleTabPlaceholder } from '@/components/shared/RoleTabPlaceholder';

export default function Routes(): React.ReactNode {
  return (
    <RoleTabPlaceholder
      screenName="Routes"
      title="Routes"
      description="Driver Manager view: build and edit delivery routes, assign drivers, balance loads."
    />
  );
}
