import { RoleTabPlaceholder } from '@/components/shared/RoleTabPlaceholder';

export default function Workers(): React.ReactNode {
  return (
    <RoleTabPlaceholder
      screenName="Workers"
      title="Workers"
      description="Warehouse staff schedule, productivity metrics, manage shifts and certifications."
    />
  );
}
